# tech_zh_b.py - 技术报告中文版：第 4-5 章
from docxkit import Report


def chapter4(R):
    R.reset_figure_counter()
    R.reset_table_counter()
    R.h1("4  系统设计与架构")

    R.h2("4.1  架构风格与被考虑的替代方案")
    R.para("系统是一个**三层分层的 Web 应用**：运行在浏览器中、不含框架的客户端；"
           "掌握全部逻辑与全部权限判定的 Flask 应用；以及由单个 SQLite 文件和上传目录构成的薄持久层。"
           "每一层只与下一层通信，客户端从不直接访问数据库。")
    R.para("该风格是针对三个替代方案比较后选定的，三次判断中的决定性因素都是第 2.5 节的约束 C1——"
           "软件必须能在评阅人的机器上用一条命令运行。")
    R.table("架构替代方案及各自的否决理由",
            ["替代方案", "吸引力", "否决理由"],
            [["三层分层（已采用）",
              "表示与逻辑分离、逻辑与存储分离；每层可独立修改；整套系统以单进程运行，无外部依赖",
              "采用。它在满足 C1 的同时仍给出清晰的关注点分离，并为权限校验提供了明确的位置。"],
             ["服务端渲染页面",
              "比客户端应用更简单：没有 JSON 契约，也不需要重复校验",
              "否决，因为界面需要在不整页刷新的情况下更新——留言区、收藏控件和聊天面板都是就地变化的，"
              "服务端渲染意味着每一次都要重载页面。"],
             ["前端分离 + 独立 API 服务",
              "关注点分离最彻底，也是生产环境中最常见形态",
              "否决，因为它需要第二个进程或跨域配置，两者都会破坏 C1；跨域请求还需显式携带会话 Cookie。"],
             ["微服务",
              "账号、商品与私信可独立部署",
              "否决，因为不成比例。整个应用约 1,700 行；拆分只会为系统并不存在的扩展问题引入服务发现、"
              "服务间契约和分布式故障模式。"]],
            widths=[1.35, 1.95, 2.9], chapter="4", size=9.5)

    R.h2("4.2  分层架构")
    R.para("图 4.1 给出三个层次及各自职责。最关键的是表示层与应用层之间的边界：客户端不掌握任何权威。"
           "所有保护数据的规则——谁能改什么、状态允许变成什么、哪些文件类型可以保存——都在边界之下执行。"
           "客户端了解这些规则只是为了给出更好的反馈，设计上也接受把部分校验写两遍的代价，"
           "以换取一个无法通过手工构造请求绕过的系统。")
    R.figure("figures_zh/fig_arch_layers.png",
             "分层架构：应用层的接口分组与数据层的组件", chapter="4")
    R.para("这一安排中有两点承载了最多设计分量。其一，表示层从不直接访问数据库——"
           "每一次读和每一次写都经由 JSON 接口，这意味着权限校验只需要在一处正确。"
           "其二，数据层刻意做薄：它只知道表和连接，不知道任何规则。"
           "这种「薄」正是将来迁移到托管数据库可以成为局部改动而非重写的原因，"
           "也是即便概念验证并不需要可移植性仍坚持做薄的理由。")
    R.table("各层职责，以及各层被禁止做的事",
            ["层次", "负责", "不得"],
            [["表示层",
              "视图状态、渲染、事件处理、即时输入校验，以及在插入文档前转义全部用户提供的文本",
              "掌握任何数据的权威；拼写 SQL；成为某条规则唯一的执行点"],
             ["应用层",
              "HTTP 路由、会话处理、权限校验、输入校验、状态机、构造 JSON 响应信封、文件存储",
              "包含表示层标记；假设请求一定来自所提供的那套客户端"],
             ["数据层",
              "创建连接、关系模式、唯一性约束、磁盘文件存储",
              "包含应用层同样需要知道的业务规则"]],
            widths=[0.8, 2.7, 2.7], chapter="4", size=9.5)

    R.h2("4.3  模块划分")
    R.para("图 4.2 给出各模块及依赖方向。划分遵循 Parnas 提出的准则——隐藏最可能变化的部分："
           "数据模型形态藏在数据层之后，HTTP 契约形态藏在客户端的请求封装之后，"
           "密码哈希与文件名净化的细节则藏在第三方库之后。")
    R.figure("figures_zh/fig_arch_modules.png",
             "模块划分与依赖方向", chapter="4")
    R.table("模块清单",
            ["模块", "行数", "职责", "依赖"],
            [["app.py", "552",
              "Flask 应用对象与全部 23 条路由：账号、目录、商品管理、上传、交易、收藏、留言、私信与个人中心",
              "models.py、Werkzeug"],
             ["models.py", "106", "连接工厂与六张表的模式创建", "sqlite3"],
             ["templates/index.html", "441",
              "唯一的文档：五个视图容器、两个模态界面、内联 SVG 图标精灵，以及客户端渲染的挂载点",
              "style.css、api.js、main.js"],
             ["static/js/api.js", "63",
              "每个接口一个方法，URL 不会被写两遍，请求形态只在一处定义", "HTTP 接口本身"],
             ["static/js/main.js", "1169",
              "全部视图状态、渲染、事件绑定、hash 路由、转义与反馈层", "api.js"],
             ["static/css/style.css", "1025", "设计令牌、组件样式、响应式断点", "无"]],
            widths=[1.2, 0.42, 3.2, 0.85], chapter="4", size=9.3)

    R.h2("4.4  领域模型")
    R.para("图 4.3 给出领域模型。之所以称其为「面向持久化」，是因为系统没有使用对象关系映射："
           "图中的类被实现为数据表以及查询返回的字典，而不是 Python 类。"
           "即便如此建模仍然值得，因为关联的基数关系正是模式必须保证的东西。")
    R.figure("figures_zh/fig_class_domain.png",
             "领域类图：六个实体及其属性与关联", chapter="4")

    R.h2("4.5  数据库设计")
    R.para("模式包含六张表，见图 4.4，已满足第三范式：每个非键属性都依赖于完整的键，与其他属性无关。"
           "设计时对照范式做了检查，其中一处候选反范式化——把封面照片的引用直接存在商品行上——被否决，"
           "改用以 images 表为目标的关联子查询，因为子查询显然正确，而冗余列需要额外代码保持同步。")
    R.figure("figures_zh/fig_er.png",
             "六张表的实体关系图，含外键与基数", chapter="4")
    R.table("数据表规格",
            ["表", "用途", "主键", "约束与说明"],
            [["users", "每个注册学生一行", "id",
              "`username` 唯一且非空；`password_hash` 非空；`nickname`、`email`、`student_id`、"
              "`avatar` 可选"],
             ["items", "每个商品一行，聚合根", "id",
              "`seller_id` 外键指向 users；`title` 非空；`price` 非空且为实数；`status` 默认在售；"
              "每次改动都写入 `updated_at`"],
             ["images", "商品的一张照片一行", "id",
              "`item_id` 外键指向 items；`file_path` 非空；存储的是生成的文件名，从不使用客户端提供的名字"],
             ["favorites", "学生与收藏商品之间的多对多关联", "id",
              "`user_id` 与 `item_id` 均为外键；`UNIQUE(user_id, item_id)` 阻止重复收藏"],
             ["comments", "挂在商品下的公开提问或回答", "id",
              "`item_id` 与 `user_id` 均为外键；`content` 非空"],
             ["messages", "两名学生之间的私信，可关联某件商品", "id",
              "`sender_id` 与 `receiver_id` 外键指向 users；`item_id` 外键指向 items 且可为空；"
              "`is_read` 默认零"]],
            widths=[0.72, 1.75, 0.32, 3.48], chapter="4", size=9.3)
    R.para("SQLite 的两个特性影响了设计，两者都作为局限而非疏漏记录在案。"
           "其一，只有当连接以 `PRAGMA foreign_keys = ON` 打开时引擎才会强制所声明外键，"
           "而概念验证并未设置该 pragma，因此当前的引用完整性是由应用层维持的约定。"
           "其二，`CURRENT_TIMESTAMP` 只精确到秒，任何仅依赖它排序的场景在两条记录写入同一秒时都不稳定——"
           "这个缺陷由测试套件发现，并在第 6.13 节报告。")

    R.h2("4.6  交易状态机")
    R.para("商品的状态字段被建模为显式的状态机，而不是一个界面碰巧会设置的自由文本字段。"
           "图 4.5 给出四个状态与五条迁移，表 4.5 给出规则。这个模型是项目最重要的设计决策，"
           "因为它让「这件还在吗」的答案从参考意见变成了权威判定。")
    R.figure("figures_zh/fig_state_item.png",
             "商品状态机：每条迁移的参与者与守卫条件", chapter="4")
    R.table("状态迁移规则。角色与原状态两者都必须满足；任一不满足的请求不改变任何数据。",
            ["操作", "原状态", "新状态", "允许的参与者", "失败响应"],
            [["reserve", "在售", "已预订", "除卖家以外的任何学生",
              "调用者是卖家时 403；状态不是在售时 400"],
             ["sell", "已预订", "已售出", "卖家", "调用者不是卖家时 403；状态不是已预订时 400"],
             ["cancel_reserve", "已预订", "在售", "卖家",
              "调用者不是卖家时 403；状态不是已预订时 400"],
             ["off_shelf", "在售", "已下架", "卖家", "调用者不是卖家时 403；状态不是在售时 400"],
             ["relist", "已下架", "在售", "卖家", "调用者不是卖家时 403；状态不是已下架时 400"],
             ["（其他）", "—", "—", "—", "在读取商品之前即返回 400 非法操作"]],
            widths=[0.95, 0.68, 0.68, 1.5, 2.46], chapter="4", size=9.3)
    R.para("「已售出」没有出边，这是刻意选择而非遗漏：撤销一次已完成的成交，需要追踪是哪位买家以什么方式完成的，"
           "而概念验证根本没有建模成交后的买家侧。其后果是误操作无法通过界面撤销，"
           "这也正是该操作的确认弹窗会明确写出「商品将被标记为已售出」的原因。")

    R.h2("4.7  交互设计")
    R.para("有三处交互承载了系统的大部分行为，因此完整建模。它们合起来行使了每一层、"
           "状态机的两个方向，以及系统中唯一一条正确性不能靠阅读直接看出的查询。")

    R.h3("4.7.1  发布商品")
    R.para("图 4.6 跟踪一次发布从表单提交到写入数据行的全过程。该时序图明确了两项设计决策。"
           "上传与创建是两个独立请求，上传先完成并返回文件名，之后才写入任何商品行；"
           "因此第二步失败时不可能留下一条引用了不存在照片的商品记录。"
           "而商品行先于照片行写入，使生成的主键可直接用作照片行的外键——反过来则需要再读一次。")
    R.figure("figures_zh/fig_seq_publish.png",
             "发布商品的时序图：从提交表单到写入数据行", chapter="4")

    R.h3("4.7.2  预订商品与确认成交")
    R.para("图 4.7 给出交易的两端。两个请求到达同一个接口，并按同一张规则表校验，"
           "这正是状态机可信的原因：两条迁移不可能各自走偏，因为它们只有一处定义。"
           "图中也显式画出了失败路径，因为「被拒绝的请求不改变任何数据」属于规格的一部分，而不是实现细节。")
    R.figure("figures_zh/fig_seq_deal.png",
             "预订商品与确认成交的时序图，含两处守卫与拒绝路径", chapter="4")

    R.h3("4.7.3  会话与私信")
    R.para("图 4.8 覆盖三处私信交互。会话列表由一条聚合查询产生，而不是每个会话查一次；"
           "这一点很重要，因为一名学生的会话列表会无限增长，而与他通信的人数并不会。"
           "打开会话同时包含读与写：消息取出与收到消息标记已读发生在同一次请求里，"
           "因此未读状态不可能与学生实际看到的内容脱节。")
    R.figure("figures_zh/fig_seq_message.png",
             "会话列表、打开会话与回复的时序图", chapter="4")

    R.h2("4.8  接口设计")
    R.para("客户端通过一组小型 JSON 接口与服务端交互。图 4.9 按作用资源对接口分组，"
           "表 4.6 列出各接口接受的方法及是否需要会话。所有接口都使用表 4.7 描述的同一种响应信封，"
           "使客户端无需检查响应体形态即可统一处理成功与失败。")
    R.figure("figures_zh/fig_route_map.png",
             "按资源分组的 JSON 接口一览，含方法与会话要求", chapter="4")
    R.table("接口清单。标记「需要」表示未登录时直接返回 401，且不解析请求体。",
            ["方法", "路径", "用途", "会话"],
            [["POST", "/api/register", "创建账号", "不需要"],
             ["POST", "/api/login", "认证并建立会话", "不需要"],
             ["POST", "/api/logout", "终止会话", "不需要"],
             ["GET", "/api/user/me", "返回调用者本人资料", "需要"],
             ["GET", "/api/items", "目录，支持关键词、分类、状态、排序与分页", "不需要"],
             ["GET", "/api/items/<id>", "商品详情，含图片与留言", "不需要"],
             ["POST", "/api/items", "创建商品", "需要"],
             ["PUT", "/api/items/<id>", "修改调用者拥有的商品", "需要"],
             ["DELETE", "/api/items/<id>", "下架商品（软删除）", "需要"],
             ["POST", "/api/upload", "存储一张或多张照片并返回文件名", "需要"],
             ["GET", "/uploads/<name>", "提供已存储的照片", "不需要"],
             ["POST", "/api/items/<id>/status", "执行一次状态迁移", "需要"],
             ["GET", "/api/favorites", "列出调用者的收藏", "需要"],
             ["POST", "/api/favorites", "收藏商品", "需要"],
             ["DELETE", "/api/favorites/<id>", "取消收藏", "需要"],
             ["GET", "/api/items/<id>/comments", "读取商品的公开留言", "不需要"],
             ["POST", "/api/items/<id>/comments", "发表留言", "需要"],
             ["GET", "/api/messages", "按对象汇总的会话列表", "需要"],
             ["POST", "/api/messages", "发送私信", "需要"],
             ["GET", "/api/messages/<uid>", "读取会话并标记已读", "需要"],
             ["GET", "/api/user/items", "调用者本人的发布", "需要"],
             ["GET", "/api/user/sold", "调用者已售出的商品", "需要"]],
            widths=[0.55, 1.75, 3.35, 0.62], chapter="4", size=9.2)
    R.table("所有接口共用的响应信封",
            ["情形", "HTTP 状态", "响应体", "客户端行为"],
            [["成功且带数据", "200", "`{ code: 200, data: … }`", "渲染数据"],
             ["成功但不带数据", "200", "`{ code: 200, msg: '…' }`", "以提示形式展示消息"],
             ["校验失败", "400", "`{ code: 400, msg: '<原因>' }`", "在出错的控件旁展示原因"],
             ["未认证", "401", "`{ code: 401, msg: '…' }`", "跳转到登录视图并记住原意图"],
             ["无权限", "403", "`{ code: 403, msg: '<原因>' }`", "展示原因，不重试"],
             ["未找到", "404", "`{ code: 404, msg: '…' }`", "展示空状态并提供返回路径"],
             ["请求体过大", "413", "框架返回的 HTML 错误页", "提示文件超过 5 MB 上限"]],
            widths=[1.15, 0.72, 1.9, 2.5], chapter="4", size=9.3)

    R.h2("4.9  客户端设计")
    R.para("客户端是单个文档，包含五个视图容器，由一个小型路由控制显示与隐藏，见图 4.10。"
           "有三项设计决策值得说明。其一，**hash 路由** `#item-<id>` 让单件商品可以被寻址，"
           "因此商品链接可以粘贴到消息中并直接打开详情页；该路由在页面加载时解析，并在每次 hash 变化时重新解析。"
           "其二，**守卫是重复的**：客户端把未登录访客从发布和个人视图重定向出去，服务端则独立拒绝同一请求。"
           "其三，**隐藏的容器不会被重建**：切换视图只重新渲染发生变化的部分，"
           "因此写到一半的商品表单在切到目录再回来时仍保留内容。")
    R.figure("figures_zh/fig_ui_states.png",
             "前端视图路由：五个视图、常驻页头，以及需要会话的视图上的守卫", chapter="4")

    R.h2("4.10  设计决策与取舍")
    R.para("表 4.8 记录塑造了系统的各项决策、各自的替代方案，以及明知代价而接受的取舍。"
           "记录被否决的替代方案很重要：一份只列结果的决策日志无法被评审，"
           "因为无从判断替代方案是被考虑后否决，还是根本没被想到。")
    R.table("设计决策日志",
            ["编号", "决策", "被否决的替代方案", "接受的代价"],
            [["D1", "由单个 Flask 进程同时提供客户端与接口",
              "独立的 API 服务或第二个进程",
              "客户端与服务端无法独立部署或扩展。接受，因为 C1 禁止第二个进程。"],
             ["D2", "以 SQLite 作为存储",
              "PostgreSQL 或托管关系数据库服务",
              "没有并发写入能力，其他客户端无法经网络访问，外键默认不强制。"
              "接受，因为模式得以随作业一起提交，且无需安装任何东西。"],
             ["D3", "不使用对象关系映射",
              "SQLAlchemy 或同类库",
              "29 条 SQL 全部手写，模式形态直接出现在路由处理函数中。"
              "接受，因为查询集合很小，且第 5.7 节的聚合查询直接书写比经过映射器更清晰。"],
             ["D4", "浏览器与服务端重复校验",
              "只在服务端校验",
              "部分规则写了两遍，两份可能走偏。接受，因为只在客户端校验等于没有校验，"
              "而只在服务端校验会让用户在得到任何反馈前先等一个来回。"],
             ["D5", "用规则表驱动状态迁移",
              "为每个操作写一串条件语句",
              "合法迁移以数据结构描述而非控制流表达，有人会觉得不够直观。"
              "接受，因为规则集可以在一处读完，且不可能与实际执行的不一致。"],
             ["D6", "商品采用软删除",
              "删除数据行及其照片",
              "已下架商品仍留在数据库中，其照片永不回收。"
              "接受，因为卖家的历史与买家的会话因此保持自洽，误删除也可恢复。"],
             ["D7", "上传文件使用生成的文件名",
              "沿用客户端提供的文件名",
              "磁盘上的文件无法通过文件名识别。接受，因为客户端提供的名字既是路径穿越风险，也可能与既有文件冲突。"],
             ["D8", "每次状态迁移都要通过两处独立守卫",
              "只做角色检查",
              "每个请求多一次读取，也多一种失败模式要测。"
              "接受，因为角色检查回答「谁」，状态检查回答「何时」，"
              "而一次预订覆盖已完成的成交是正确性故障。"],
             ["D9", "会话列表使用单条聚合查询",
              "每个会话查一次，或缓存列表",
              "该查询比简单查询更难读。"
              "接受，因为它使成本不随会话数量增长，而替代方案会为列表的每一行各查一次。"],
             ["D10", "不提供支付能力",
              "接入支付服务商",
              "平台无法抽佣，交付必须由两名学生自行约定。"
              "刻意接受：支付会把金融监管、纠纷处理与欺诈防范带进一个课程项目。"]],
            widths=[0.32, 1.35, 1.15, 3.45], chapter="4", size=9)


def chapter5(R):
    R.reset_figure_counter()
    R.reset_table_counter()
    R.h1("5  实现")

    R.h2("5.1  技术栈")
    R.para("概念验证用 Python 与 Flask 编写，使用 SQLite 存储，并提供一套由纯 HTML、CSS 与 JavaScript "
           "构成、不含框架也没有构建步骤的客户端。图 5.1 给出每项选择的理由；所有组件都用同一条标准筛选——"
           "评阅人能否在不买账号、不装容器、不做编译的前提下把它跑起来？")
    R.figure("figures_zh/fig_stack.png", "技术选型与理由", chapter="5")
    R.table("组件版本与作用",
            ["组件", "版本", "在系统中的角色"],
            [["Python", "3.8 及以上（开发环境为 3.13）", "应用与测试套件的运行时"],
             ["Flask", "2.3.3", "路由、请求解析、签名会话与 JSON 响应"],
             ["Werkzeug", "2.3.7", "密码哈希与文件名净化"],
             ["SQLite", "3（随 Python 提供）", "关系存储，磁盘上单个文件"],
             ["pytest", "9.x", "第 6 章所述测试套件的运行器"],
             ["Playwright（Node）", "playwright-core", "驱动 Microsoft Edge 完成界面验证与本报告中的界面抓取"]],
            widths=[1.2, 1.3, 3.77], chapter="5")

    R.h2("5.2  软件结构与规模")
    R.para("表 5.2 给出所交付软件各部分的规模。分布本身值得注意：客户端比服务端更长，"
           "这也是一个复杂度落在交互而非计算上的系统的预期形态。服务端以 658 行实现 21 个 JSON 接口，"
           "平均每个接口 31 行，其中已包含校验与权限判定。")
    R.table("所交付软件各部分的规模",
            ["组成部分", "行数", "占比"],
            [["static/js/main.js", "1,169", "34%"],
             ["static/css/style.css", "1,025", "30%"],
             ["app.py", "552", "16%"],
             ["templates/index.html", "441", "13%"],
             ["models.py", "106", "3%"],
             ["static/js/api.js", "63", "2%"],
             ["tests/（测试套件与测量脚本）", "约 1,300", "不计入运行时"],
             ["**运行时合计**", "**3,356**", "**100%**"]],
            widths=[2.6, 1.2, 1.0], chapter="5", align=[None, "r", "r"])

    R.h2("5.3  账号、会话与凭据存储")
    R.para("注册接受用户名、密码与三个可选资料字段。密码从不存储：它经过带盐的密钥派生函数处理，"
           "只把结果哈希写入数据库。实现刻意使用该库的默认参数而不自行选择，"
           "遵循的原则是自研密钥派生函数本身就是负债。有一个测试断言存储值既不包含提交的密码，"
           "又带有预期的算法前缀，因此将来若有人误存明文，测试会失败而不是无声通过。")
    R.para("会话保存在 Flask 的签名会话 Cookie 中。应用对它的唯一操作是在登录时写入已认证用户标识、"
           "在登出时将其移除。一个装饰器被施加到每个受保护路由上，读取该标识并在缺失时以 401 拒绝请求。"
           "由于装饰器施加在路由层面而不是在函数内部判断，新增接口若忘记加它会在评审中立刻显现——"
           "而第 6.7 节的权限矩阵正是为了让这种失败模式不可能被忽略。")

    R.h2("5.4  商品目录查询")
    R.para("目录接口接受五个可选参数——关键词、分类、状态、排序与页码——并据此构造一条语句。"
           "构造方式是把条件片段追加到列表、并同步扩展一个平行的绑定参数列表，"
           "绝不把取值插进语句文本。这一区别很关键：拼接关键词会让搜索框成为注入点，而绑定不会。"
           "同一份条件列表还被复用来构造计数查询，因此驱动分页控件的总数与它所描述的那一页用的是完全相同的筛选条件。")
    R.para("关键词在两个列上做两侧通配匹配，这正是搜索「laptop」能找到「Lenovo laptop bag」的原因。"
           "每件商品的封面照片用以 images 表为目标的关联子查询取得，而不是用连接，"
           "因为连接会让商品行按照片数量重复，而目录页每张卡片只需要一张图。"
           "两侧通配的代价是全表扫描，因为前置通配无法使用索引；在概念验证的规模下这无关紧要，"
           "第 6.10 节对此有测量；在更大规模上，答案是全文索引，而不是修改这条查询。")

    R.h2("5.5  照片上传与存储")
    R.para("上传是系统中唯一触及文件系统的部分，因此被严肃对待。接口接收包含一个或多个文件的"
           "multipart 请求，逐个检查扩展名是否在白名单内，从净化后的文件名中取出扩展名，"
           "然后以一个由随机通用唯一标识生成的名字写入文件。客户端提供的名字除了用来读取扩展名之外不被用于任何事。")
    R.table("上传与发布所应用的校验规则及各自的执行位置",
            ["规则", "限制", "浏览器端执行", "服务端执行"],
            [["接受的文件类型", "png、jpg、jpeg、gif、webp", "按 MIME 类型在选择时校验",
              "按扩展名对照白名单校验"],
             ["单个文件大小上限", "5 MB", "选择时校验", "由框架的请求体大小限制执行（413）"],
             ["每件商品的照片数上限", "3", "选择时校验", "未执行；该限制只在客户端生效"],
             ["标题必填", "非空", "提交前校验", "写入数据行前校验"],
             ["价格必填且为数字", "不小于零", "提交前校验", "写入数据行前校验"],
             ["至少一张照片", "1", "提交前校验", "未执行；商品也可以合理地不带照片"]],
            widths=[1.35, 1.15, 1.5, 2.27], chapter="5", size=9.3)
    R.para("表 5.3 的最后两行是如实记录的空缺。三张照片上限与「至少一张照片」只存在于客户端。"
           "手工构造的请求可以发布一件没有照片、或带三十张照片的商品。两处遗漏都不会破坏数据，"
           "界面也不会产生这样的请求，因此概念验证接受了这一风险，"
           "并将其列为待修复缺陷，而不是当作对 FR-06 的完整实现来呈现。")

    R.h2("5.6  状态机")
    R.para("状态机实现为一个字典，把每个操作映射到它要求的原状态、结果状态以及被允许执行的角色。"
           "接口随后对每个操作执行同样的五步，见图 5.2：拒绝未知操作、读取商品并拒绝不存在的商品、"
           "检查角色、检查原状态，最后才写入。由于这五步是共用的，"
           "新增一个状态只需在表里加一项、在界面加一个控件，而无需改动校验逻辑。")
    R.figure("figures_zh/fig_algo_status.png",
             "状态变更请求在写库前的校验流程，由一张五条目的规则表驱动", chapter="5")
    R.para("这一实现中有两点值得为之辩护。操作在**读取商品之前**就对照规则表校验，"
           "因此未知操作零成本，也不会泄露商品是否存在。而写入是最后一步、其后没有任何条件逻辑，"
           "这正是测试所断言的那条性质的保证：任何一步失败都让数据行保持原样。")

    R.h2("5.7  会话列表查询")
    R.para("会话列表是系统中唯一一个真正需要动脑的算法。需求是每个正在通信的人显示一行，"
           "内容是该会话中最新的一条消息。最直观的实现——遍历该学生的消息、为每条算出对方、"
           "按人保留最新一条——需要应用层一次完整遍历，并且要么为每个会话各查一次，"
           "要么把所有消息读进内存。图 5.3 给出取而代之的单条聚合查询。")
    R.figure("figures_zh/fig_algo_conversation.png",
             "用一条聚合查询而非按会话各查一次来渲染会话列表", chapter="5")
    R.para("该查询分两部分。内层按对方对学生自己的消息分组，用一个条件表达式把「发送者与接收者」"
           "这一对规范化成单一身份（与方向无关），再选出每组中最大的标识。"
           "外层以该标识回连 messages 表，并连到 users 表取得对方昵称。"
           "成本是一次遍历加一次分组子查询，且不随会话数量增长。")

    R.h2("5.8  客户端实现")
    R.para("客户端是单个文档，包含五个视图容器、两个模态界面、一个内联图标精灵，以及渲染代码所填充的挂载点。"
           "全部渲染由模块级变量中的状态驱动，而每一条把用户文本插入文档的路径都先经过转义函数。"
           "该函数是系统防范跨站脚本的主要手段，被应用于标题、描述、留言正文、消息正文、昵称与文件名。")
    R.table("客户端各渲染路径及其所应用的转义",
            ["渲染路径", "被插值的用户提供值", "转义情况"],
            [["商品卡片", "标题、卖家昵称、价格、分类",
              "全部转义；价格在格式化前先强制转为数字"],
             ["商品详情", "标题、描述、卖家昵称与邮箱、分类、成色", "全部转义"],
             ["留言列表", "作者昵称、正文、相对时间", "全部转义；时间经格式化，从不原样插入"],
             ["聊天与会话列表", "消息正文、对方昵称", "全部转义"],
             ["发布预览", "标题、价格、分类", "全部转义；预览与目录卡片由同一函数生成"],
             ["个人资料头部", "昵称、用户名、学号", "全部转义"],
             ["提示与确认弹窗", "服务端返回的消息",
              "提示做转义；确认弹窗通过 `textContent` 赋值，本身无法创建标记"]],
            widths=[1.35, 2.0, 2.92], chapter="5", size=9.3)

    R.h2("5.9  设计体系")
    R.para("视觉语言只定义一次，作为文档根节点上的自定义属性，所有组件样式引用这些属性而不是字面值。"
           "实际收益是整套主题可以在一处调整；评审上的收益则是硬编码颜色的组件在阅读文件时会显得突兀。")
    R.table("设计令牌分组及其控制的内容",
            ["分组", "令牌", "用于"],
            [["颜色", "背景、次级背景、边框、正文、次要正文、主色、主色浅底、成功、危险、警告",
              "界面中所有背景、边框、文字颜色与状态徽章"],
             ["字体", "正文两档、标题四档、一种等宽字体、统一的字号级差",
              "所有文字；没有组件在字号级差之外自定义字号"],
             ["间距", "从 4 像素到 40 像素的五级间距", "所有外边距、内边距与网格间隙"],
             ["圆角与阴影", "三档圆角、三档阴影", "卡片、按钮、弹窗与粘性购买面板"],
             ["断点", "1040 像素、900 像素、760 像素",
              "详情页收敛为单栏、网格收敛为两列、页头收敛为紧凑形态"]],
            widths=[1.05, 2.3, 2.92], chapter="5", size=9.3)

    R.h2("5.10  用户界面")
    R.para("界面以一组状态而非每个界面一张截图来呈现，因为设计工作量最大的状态恰恰是只在空白或被拒绝时才出现的那些。"
           "以下每张图都取自所交付软件驱动真实浏览器运行的结果，视口宽度 1240 像素，"
           "抓取过程无控制台报错、无非预期的 4xx 或 5xx 响应。图中按讨论对象做了裁切："
           "整页截图缩到印刷栏宽后正文字号只有约 5 磅，无法阅读，而裁切后以较小的物理宽度插入可保持 8 至 9 磅。")

    R.h3("5.10.1  浏览目录")
    R.para("目录是入口，且必须对没有账号的访客可用。图 5.4 给出商品网格与排序控件。"
           "卡片承载状态徽章、封面图、价格、卖家与分类，这正是第 2.2 节竞品观察中确认的、"
           "买家用来筛选商品的那组属性。")
    R.figure("figures_zh/ui_crop_grid.png",
             "商品目录网格：每张卡片上的状态徽章、封面图、价格、卖家与分类",
             chapter="5", width_in=6.2)
    R.para("图 5.5 给出空状态，这是刻意收录的——空状态正是同类项目通常止步的地方：网格保持空白，"
           "用户无法判断是没有匹配结果还是出了故障。本实现会说明具体情形并给出下一步。")
    R.figure("figures_zh/ui_crop_empty.png",
             "搜索无结果的空状态：说明情形并给出下一步，而不是留下空白网格",
             chapter="5", width_in=5.4)

    R.h3("5.10.2  查看商品并采取操作")
    R.para("详情页是信息最密集的界面，也是状态机对用户变得可见的地方。图 5.6 与图 5.7 展示同一件商品"
           "在交易两端的形态。对已登录且非卖家的买家，可用操作是预订、收藏与联系卖家，"
           "安全提示明确写出平台对支付的态度。对卖家，可用操作完全不同：在售时可下架，"
           "已预订时可确认成交与取消预订。界面是按角色区分的，但界面背后的权威不是——"
           "即使控件出现在错误的账号上，服务端也会拒绝同一操作，这正是第 6.7 节权限矩阵所确立的。")
    R.figure("figures_zh/ui_crop_buy_panel.png",
             "已登录买家看到的详情页：预订、收藏与联系卖家，以及卖家与结构化属性",
             chapter="5", width_in=4.3)
    R.figure("figures_zh/ui_crop_sell_panel.png",
             "同一界面对卖家：购买操作被管理操作取代", chapter="5", width_in=4.3)
    R.para("图 5.8 给出守护下架操作的确认弹窗。界面中每一个破坏性或难以撤回的操作都会经过这个弹窗，"
           "它说明将会发生什么，而不是仅问一句「确定吗」。")
    R.figure("figures_zh/ui_crop_confirm.png",
             "界面中所有破坏性操作共用的确认弹窗", chapter="5", width_in=4.4)

    R.h3("5.10.3  发布")
    R.para("图 5.9 给出发布表单。其中可见三项决策。分类与成色使用芯片式选择器而非下拉框，"
           "并与隐藏的 select 元素保持同步，使表单在没有脚本时仍可提交。照片控件是一个既能点击也能拖放的区域，"
           "缩略图可逐张删除。表单旁是商品卡片的实时预览，由渲染目录卡片的同一函数生成，"
           "因此卖家看到的就是买家将看到的。")
    R.figure("figures_zh/ui_crop_publish.png",
             "发布表单：分类与成色的芯片选择器、照片拖放区，以及挂在出错字段上的校验提示",
             chapter="5", width_in=4.6)

    R.h3("5.10.4  个人中心与私信")
    R.para("个人中心把属于已登录学生的一切收在四个标签页之后。图 5.10 给出带三个统计数的资料头部"
           "与「我的发布」标签页；收藏与已售出标签页复用与目录相同的卡片组件，"
           "这也是三处网格看起来完全一致的原因。")
    R.figure("figures_zh/ui_crop_profile.png",
             "个人中心：带统计数的资料摘要，以及学生本人的发布", chapter="5", width_in=6.0)
    R.para("图 5.11 给出一个打开的会话。本人与对方的消息采用不同的对齐方式，并标注相对时间；"
           "面板滚动到最新一条，回车即可发送。该面板被标注为「私信」而不是「实时聊天」，"
           "因为实现只在打开与发送后拉取，并不轮询——这是刻意选择，"
           "因为轮询会把系统的复杂度预算花在评估并不要求的功能上。"
           "相对时间在浏览器中由存储的时间戳计算，解析时先把值显式归一化为 UTC，"
           "因为数据库存储的时间戳不带时区，直接解析会产生一个恰好等于本机与 UTC 偏移量的误差。")
    R.figure("figures_zh/ui_crop_chat.png",
             "打开的会话：区分本人与对方的消息、相对时间，以及固定在底部的输入框",
             chapter="5", width_in=4.3)

    R.h3("5.10.5  小屏")
    R.para("图 5.12 给出 400 像素视口下的界面，这是大屏手机的典型宽度。网格收敛为两列，"
           "页头缩减为品牌、搜索框与一个紧凑操作；详情页（未展示）收敛为单栏，购买面板位于图廊下方。"
           "该视口正是 NFR-05 所用的验证视口。")
    R.figure("figures_zh/ui_mobile_home.png",
             "400 像素视口下的界面：网格收敛为两列，页头缩减为必要控件",
             chapter="5", width_in=2.7)

    R.h2("5.11  编码规范与约定")
    R.para("代码遵循一组在实现之前商定、并在评审中检查的约定。记录在此是因为它们本身也是软件工程评估所考察的内容，"
           "而没有写下来的约定无法被强制执行。")
    R.table("在整个代码库中应用的编码约定",
            ["约定", "理由"],
            [["所有 SQL 语句都绑定参数，任何取值都不插进语句文本",
              "把注入作为一类缺陷整体消除，而不是逐个防御"],
             ["所有响应使用同一种 `{ code, msg, data }` 信封",
              "客户端可以在一处处理成功与失败，而不必检查响应体形态"],
             ["每个受保护路由都带同一个权限装饰器",
              "缺失守卫在路由定义处即可看出，而不是隐藏在处理函数内部"],
             ["数据库连接在用到它的请求内打开与关闭，且包括出错路径在内每条路径都要关闭",
              "SQLite 是文件型的，泄漏的连接占住的是锁而不是套接字"],
             ["客户端代码把所有插值都经过同一个共享转义函数",
              "只需在一处写对，且被转义的内容有一份可评审的清单"],
             ["除令牌确实无意义的场合外，不写出令牌集之外的 CSS 取值",
              "保持视觉语言一致，并让偏离一眼可见"],
             ["数据层中不通过字符串拼接构造 SQL 或 HTML",
              "保持分层的诚实：数据层知道表，不知道标记"]],
            widths=[2.7, 3.57], chapter="5", size=9.5)

    R.h2("5.12  部署与可复现性")
    R.para("应用以单条命令 `python app.py` 启动，适用于任何装有 Python 3.8 及以上版本"
           "与两个已声明依赖的机器。首次运行时它会创建数据库文件与上传目录，"
           "因此一次干净的检出即可成为可运行的系统，无需迁移或初始化步骤。"
           "仓库自带一份演示数据集，使每个界面都有内容可展示，并附带空库备份，"
           "因此无需改动模式即可移除演示数据。")
