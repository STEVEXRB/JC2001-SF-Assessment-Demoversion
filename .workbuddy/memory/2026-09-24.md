# 2026-09-24

## 修复实例中的图片资源（演示封面 404）

- 根因：`seed.py` 往 `images` 表写入 9 个 `uploads/demo-*.svg` 文件名，但 `uploads/` 被 gitignore，SVG 从未提交，导致全新克隆/本机 uploads 目录为空，所有商品封面 404（README 却声称这些文件随仓库发布）。
- 修复：
  1. 新增 `demo_covers.py`：手写的 9 张 800x600 (4:3) 扁平 SVG 插画（book/laptop/headphones/mouse/lamp/backpack/bicycle/racket/calculator），配色对齐 `style.css` 的 `:root` 设计变量；`write_covers(dest, force=False)` 幂等写入，已存在文件不覆盖（保护用户替换过的真实照片）。
  2. `seed.py`：新增 `--upload-dir` 参数并在 seed 时调用 `write_covers`，使 `python seed.py` 真正可复现；`--force` 路径会补齐缺失封面。
  3. 前端兜底：`main.js` 用 capture 阶段的 `error`/`load` 监听，把加载失败的 `/uploads/` 图片替换为现有 `.no-img` 占位块（图库主图带 "Photo unavailable" 文案），`style.css` 增加 `img.is-broken{display:none}` 与通用 `.no-img` 规则。原因：error 不冒泡必须 capture；图库缩略图切换用 `$('gallery-main-img').src`，故采用"隐藏 img + 插占位兄弟节点 + load 恢复"而非删除节点。
  4. 回归测试 `tests/test_demo_covers.py`（6 个用例）：seed 后 image rows 全部落盘、SVG 合法 (viewBox 800x600)、write_covers 幂等、重播种不覆盖用户文件、路由 200/404。
- 验证：pytest 86 全通过（原 80 + 新 6）；`seed.py --force` 输出 "covers 0 written, 9 already present"；无头 Chrome 截图确认首页 8 张封面 + 1 个无图占位正常；`/uploads/demo-*.svg` 全部 200。
- 环境注意：系统 python 无 Flask/pytest，本项目用隔离 venv：`C:/Users/Huawei/.workbuddy/binaries/python/envs/default/Scripts/python.exe`（已装 Flask 2.3.3 / Werkzeug 2.3.7 / pytest）。
- README 已同步：demo_covers.py 结构条目、封面生成说明、测试计数 80→86。

## 补充：textbook 分类下"笔记"商品无图

- 用户反馈 textbook 图片未呈现。排查结论：教材《Data Structures...》封面一直正常；无图的是同分类的另一件商品《Introduction to Software Engineering - lecture notes》——seed 数据里**故意** cover=None 用于演示空图库占位。
- 处理：新增第 10 张封面 `demo-notes.svg`（纸堆+钢笔+荧光笔线条插画）并绑定到该商品；其描述里"no photos yet..."文案同步删除。空图库占位演示改由 OFF_SHELF 的 drafting kit 承担（个人中心可见）。
- 同步更新：seed.py COVER['notes']、README（10 covers、One of the eleven...）。
- 经验：Bash 工具里 `(cmd &)` 起的常驻服务会在命令结束后被回收，跨命令存活的 server 要用 run_in_background=true。
